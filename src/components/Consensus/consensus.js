import { Box, Typography} from '@mui/material';

function Consensus() {
  return (
    <Box>
      <Typography variant='body1'>With all the given information, it's difficult select a "best" option without fully knowing what the future plans are. However, for the time being, we think the best option is: </Typography>
    </Box>
  );
}

export default Consensus;